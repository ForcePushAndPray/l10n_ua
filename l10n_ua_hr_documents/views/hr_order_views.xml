<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="hr_order_view_form" model="ir.ui.view">
        <field name="name">hr.order.form</field>
        <field name="model">hr.order</field>
        <field name="arch" type="xml">
            <form string="HR Order">
                <header>
                    <button name="action_confirm" string="Confirm" type="object"
                            invisible="state != 'draft'"/>
                    <button name="action_sign" string="Sign" type="object"
                            class="btn-primary" invisible="state != 'confirmed'"/>
                    <button name="action_print" string="Print" type="object"
                            invisible="state == 'draft'"/>
                    <button name="action_cancel" string="Cancel" type="object"
                            invisible="state in ('signed', 'cancelled')"/>
                    <button name="action_draft" string="Set to Draft" type="object"
                            invisible="state != 'cancelled'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,signed"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="order_type_id"/>
                            <field name="category" invisible="1"/>
                            <field name="number"/>
                            <field name="date"/>
                        </group>
                        <group>
                            <field name="employee_id"/>
                            <field name="department_id"/>
                            <field name="job_id"/>
                            <field name="contract_id"/>
                        </group>
                    </group>
                    <group invisible="category != 'hiring'">
                        <group string="Hiring Details">
                            <field name="hire_date"/>
                            <field name="probation_days"/>
                            <field name="wage"/>
                        </group>
                    </group>
                    <group invisible="category != 'termination'">
                        <group string="Termination Details">
                            <field name="termination_date"/>
                            <field name="termination_reason_id"/>
                        </group>
                    </group>
                    <group invisible="category != 'transfer'">
                        <group string="Transfer Details">
                            <field name="new_department_id"/>
                            <field name="new_job_id"/>
                            <field name="new_wage"/>
                        </group>
                    </group>
                    <group invisible="category not in ('vacation', 'business_trip')">
                        <group string="Period">
                            <field name="date_from"/>
                            <field name="date_to"/>
                        </group>
                    </group>
                    <group invisible="category != 'bonus'">
                        <group string="Bonus Details">
                            <field name="bonus_amount"/>
                            <field name="bonus_reason"/>
                        </group>
                    </group>
                    <group>
                        <field name="basis"/>
                    </group>
                    <notebook>
                        <page string="Order Body" name="body">
                            <field name="body"/>
                        </page>
                        <page string="Signature" name="signature" invisible="state != 'signed'">
                            <group>
                                <field name="signed_by"/>
                                <field name="signed_date"/>
                            </group>
                        </page>
                        <page string="Notes" name="notes">
                            <field name="notes"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="hr_order_view_tree" model="ir.ui.view">
        <field name="name">hr.order.tree</field>
        <field name="model">hr.order</field>
        <field name="arch" type="xml">
            <tree string="HR Orders" decoration-muted="state == 'cancelled'">
                <field name="number"/>
                <field name="date"/>
                <field name="order_type_id"/>
                <field name="employee_id"/>
                <field name="department_id" optional="show"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'signed'"
                       decoration-info="state == 'draft'"
                       decoration-warning="state == 'confirmed'"
                       decoration-muted="state == 'cancelled'"/>
            </tree>
        </field>
    </record>

    <record id="hr_order_view_search" model="ir.ui.view">
        <field name="name">hr.order.search</field>
        <field name="model">hr.order</field>
        <field name="arch" type="xml">
            <search string="Search Orders">
                <field name="number"/>
                <field name="employee_id"/>
                <field name="order_type_id"/>
                <separator/>
                <filter name="filter_draft" string="Draft" domain="[('state', '=', 'draft')]"/>
                <filter name="filter_signed" string="Signed" domain="[('state', '=', 'signed')]"/>
                <separator/>
                <filter name="filter_hiring" string="Hiring" domain="[('category', '=', 'hiring')]"/>
                <filter name="filter_termination" string="Termination" domain="[('category', '=', 'termination')]"/>
                <filter name="filter_vacation" string="Vacation" domain="[('category', '=', 'vacation')]"/>
                <separator/>
                <group expand="0" string="Group By">
                    <filter name="group_type" string="Order Type" context="{'group_by': 'order_type_id'}"/>
                    <filter name="group_employee" string="Employee" context="{'group_by': 'employee_id'}"/>
                    <filter name="group_state" string="Status" context="{'group_by': 'state'}"/>
                    <filter name="group_month" string="Month" context="{'group_by': 'date:month'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_hr_order" model="ir.actions.act_window">
        <field name="name">HR Orders</field>
        <field name="res_model">hr.order</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id" ref="hr_order_view_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create an HR order
            </p>
        </field>
    </record>

</odoo>
