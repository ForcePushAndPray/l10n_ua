<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="hr_report_wizard_view_form" model="ir.ui.view">
        <field name="name">hr.report.wizard.form</field>
        <field name="model">hr.report.wizard</field>
        <field name="arch" type="xml">
            <form string="Generate Report">
                <group>
                    <group>
                        <field name="report_type"/>
                        <field name="year"/>
                        <field name="quarter" invisible="report_type != '1df'"/>
                        <field name="month" invisible="report_type not in ('d5', 'headcount', 'wage_fund')"/>
                    </group>
                    <group>
                        <field name="date_from" invisible="report_type not in ('headcount', 'wage_fund')"/>
                        <field name="date_to" invisible="report_type not in ('headcount', 'wage_fund')"/>
                        <field name="company_id" groups="base.group_multi_company"/>
                    </group>
                </group>
                <footer>
                    <button name="action_generate_report" string="Generate" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_hr_report_wizard" model="ir.actions.act_window">
        <field name="name">Generate Report</field>
        <field name="res_model">hr.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
