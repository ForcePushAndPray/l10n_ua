# HOWTO: Банки та платежі — робочі флоу

← [Назад до документації](../../README.md) · [Опис домену](../banks.md)

> Покрокові **робочі процеси** з реальним наповненням даних і результатами введення,
> звірені на **[demo.ndev.online](https://demo.ndev.online)**
> (компанія _ТОВ «Тестова Компанія»_, вхід `demo`/`demo`). Дані на демо **не видаляються**,
> тож базу можна наповнювати під час тестування.
>
> Опис моделей і точок розширення — див. [Опис домену «Банки та платежі»](../banks.md).

Скріншоти — у теці [`img/banks/`](img/banks/) (імена файлів вказані біля кроків).

Модулі домену (версії з маніфестів):

| Модуль | Версія | Призначення |
|--------|--------|-------------|
| `l10n_ua_bank_sync` | 19.0.1.0.0 | База: конфіг підключення, завдання, виписки, транзакції, правила співставлення |
| `l10n_ua_bank_privat` | 19.0.1.0.0 | ПриватБанк (Автоклієнт Privat24 Business + legacy Merchant API) |
| `l10n_ua_bank_mono` | 19.0.1.0.0 | monobank (Personal Statement API) |
| `l10n_ua_bank_currency_sync` | 19.0.1.0.0 | Курси валют (НБУ / ПриватБанк / monobank) |
| `l10n_ua_payment_link` | 19.0.1.0.0 | QR-посилання НБУ + еквайринг monobank |

Меню застосунків → розділ **Банк UA** (додається модулем `l10n_ua_bank_sync`): конфігурації
синхронізації, завдання синхронізації, виписки, транзакції, правила співставлення, а також
курси валют (від `l10n_ua_bank_currency_sync`).

> _Скриншот:_ `00-menu.png`

---

## Передумови (зовнішнє підключення)

> **Важливо — що можна пройти наживо на демо, а що ні.**
>
> | Флоу | Потрібні зовнішні ключі? | Наживо на демо |
> |------|--------------------------|----------------|
> | **Флоу 1. Курси валют НБУ** | ❌ Ні (публічний ендпоінт `bank.gov.ua`) | ✅ Проходиться повністю |
> | **Флоу 2. ПриватБанк** | ✅ Токен Автоклієнта або merchant-дані | ⛔ Тільки налаштування; завантаження виписки — ні |
> | **Флоу 3. monobank** | ✅ Персональний токен `api.monobank.ua` | ⛔ Тільки налаштування; завантаження виписки — ні |
> | **Флоу 4. QR-посилання** | QR НБУ — ❌ ні; еквайринг mono — ✅ токен мерчанта | ✅ QR НБУ так; оплата mono — ні |
>
> Банківські інтеграції ПриватБанк і monobank звертаються до **реальних API банків** з вашим
> токеном. Цих ключів **немає на демо-сервері**, тому реальне завантаження виписок наживо на демо
> **не виконується** — відповідні кроки нижче описують налаштування, а живий запуск потребує
> ваших власних облікових даних. **Синхронізація курсів НБУ винятків не має — вона працює без
> жодних ключів.**
>
> Що потрібно отримати заздалегідь:
>
> - **ПриватБанк (Автоклієнт):** у кабінеті **Privat24 for Business → «Автоклієнт»** увімкнути
>   доступ, згенерувати **токен** і скопіювати **IBAN рахунку**. Для старих інтеграцій — merchant id
>   + пароль + номер картки (legacy Merchant API).
> - **monobank:** персональний **токен** на [api.monobank.ua](https://api.monobank.ua) (кнопка
>   «Отримати токен» через застосунок). Токен дає доступ до `personal/statement`.
> - **Еквайринг monobank** (для платіжних посилань): окремий токен мерчанта monobank Acquiring,
>   що вписується у поле банківського журналу.

---

## Флоу 1. Синхронізація курсів валют НБУ

> Наскрізний процес «джерело курсів → синхронізація → курси у `res.currency.rate`».
> **Працює без будь-яких ключів** і повністю відтворюється наживо на демо
> (модуль `l10n_ua_bank_currency_sync`, модель `res.currency.rate.provider`).

### Крок 1. Активувати валюти

**Бухгалтерія → Конфігурація → Валюти** (або меню валют). Увімкніть валюти, курси яких треба
тягнути (USD, EUR тощо) — прапорцем **Активна**. Неактивні валюти в синхронізацію не потрапляють.

> Компанія демо працює у **UAH**. Для не-гривневих компаній модуль коректно перераховує
> крос-курс (`_uah_per_company_unit`, `_rate_vals`), тож курс завжди зберігається відносно
> валюти компанії.

### Крок 2. Створити провайдера курсів

**Банк UA → Курси валют → `Новий`** (модель `res.currency.rate.provider`). Заповніть:

| Поле | Значення |
|------|----------|
| Назва (`name`) | Курси НБУ |
| Провайдер (`code`) | **НБУ** (`nbu`) |
| Валюти для синхронізації (`currency_ids`) | _порожньо_ = всі активні (або оберіть USD, EUR) |
| Авто-синхронізація (`auto_sync`) | ☐ (увімкнути для планового крону) |

> Альтернативні джерела того ж поля `code`: **ПриватБанк** (`privatbank`, з вибором типу курсу
> `privat_rate_type`: готівковий `5` / безготівковий `11`) та **monobank** (`monobank`). Обмеження
> — один провайдер кожного типу на компанію (`_unique_code_company`).

> _Скриншот:_ `07-currency-provider.png`

### Крок 3. Запустити синхронізацію (результат введення)

Натисніть **`Синхронізувати`** (`action_sync_rates()`, або майстер синхронізації). Система звертається
до ендпоінта НБУ
`https://bank.gov.ua/NBUStatService/v1/statdirectory/exchange?json&date=YYYYMMDD`
і записує курси. Очікуваний **результат**:

| Показник | Результат |
|----------|-----------|
| Записи у `res.currency.rate` | створено/оновлено на дату синхронізації |
| Поле **Остання синхронізація** (`last_sync_date`) | оновлено поточним часом |
| Курс USD/EUR у картці валюти | відповідає офіційному курсу НБУ на дату |
| Історія курсів | зберігається (по одному запису на дату) |

Для планового оновлення увімкніть **cron** синхронізації (або `auto_sync` на провайдері).

> Модуль покрито тестами `tests/test_cross_rate.py` (8 сценаріїв: реципрокність гривні,
> крос-курс для не-UAH компанії, ігнорування «сирих» котирувань, крос-курс monobank тощо).

> _Скриншот:_ `08-currency-rates.png`

---

## Флоу 2. ПриватБанк (Автоклієнт / API)

> Імпорт банківської виписки ПриватБанку в проводки (модуль `l10n_ua_bank_privat`, провайдер
> `privat`). **Наживо на демо не проходиться повністю** — крок 2 (завантаження) потребує реального
> токена Автоклієнта. Кроки 1 і 3 описують налаштування та очікуваний результат.

### Крок 1. Ввести облікові дані у конфігурації синхронізації

**Банк UA → Конфігурації синхронізації → `Новий`** (модель `l10n_ua.bank.sync.config`).
Одна конфігурація = один банківський рахунок. Заповніть:

| Поле | Значення |
|------|----------|
| Провайдер (`provider`) | **PrivatBank** (`privat`) |
| Банківський журнал (`journal_id`) | банківський журнал ПриватБанку (тип `bank`) |
| **API Token** (`privat_api_token`) | токен з Privat24 Business → Автоклієнт |
| **Account IBAN** (`privat_account_iban`) | UA + 27 цифр |
| _(legacy)_ Merchant ID / Password / Card (`privat_merchant_*`) | лише для старого Merchant API |

> Гілка отримання виписок обирається **за наявними даними**: якщо є `privat_api_token` — сучасний
> **Автоклієнт**; якщо лише merchant-дані — legacy. Банківський журнал прив'язується до конфігу;
> `res.bank` зберігає код **МФО**, `res.partner.bank` валідує **IBAN**.

Натисніть **`Перевірити підключення`** (`action_test_connection()`) — звернення до API з токеном.

> **Наживо на демо не виконується** — потрібен реальний токен Автоклієнта.

> _Скриншот:_ `05-privat-config.png`

### Крок 2. Завантажити виписку (потрібен токен)

**Банк UA → Завдання синхронізації** (модель `l10n_ua.bank.sync.job`, машина станів
`draft → fetching → fetched → processing → done` / `error`):

1. Створіть завдання від конфігурації, вкажіть **період** (`date_from` / `date_to`).
2. **`Завантажити`** — `_fetch_from_bank()` → `_privat_fetch_autoclient()`:
   `GET https://acp.privatbank.ua/api/statements/transactions` із заголовком **`token`** і
   параметрами `startDate` / `endDate` / `acc`. Сирий payload зберігається (`payload_fetched_at`).
   _(Legacy-гілка `_privat_fetch_merchant()` → `https://api.privatbank.ua/p24api/rest_fiz`,
   XML-запит `cmt`.)_
3. **`Обробити`** (`action_process`) — `_parse_transactions()` розбирає payload у виписку
   (`l10n_ua.bank.statement`) й окремі транзакції (`l10n_ua.bank.transaction`).

> Сирий payload зберігається — повторну обробку можна запустити **без повторного завантаження**
> з банку (зручно при відлагодженні правил).

> **Наживо на демо не виконується** — крок вимагає реального токена.

> _Скриншот:_ `02-sync-job.png`, `03-transactions.png`

### Крок 3. Результат: транзакції імпортовано, рознесення по рахунках

Очікуваний **результат** після обробки:

| Показник | Результат |
|----------|-----------|
| Виписка (`l10n_ua.bank.statement`) | створена за період, з підсумками |
| Транзакції (`l10n_ua.bank.transaction`) | по одній на операцію; поля контрагента (назва, IBAN, ЄДРПОУ) |
| Контрагент `partner_id` | підв'язаний `_match_partner()` за **ЄДРПОУ → IBAN** (див. Флоу 5в) |
| Стан транзакції `state` | `new` → `draft` (є чернетка проводки) → `posted` (проведено) — обчислюється зі `move_id.state` |
| Кнопка **`Створити проводку`** (`action_create_move()`) | формує `account.move`; за збігом правила проставляється `matched_rule_id` |

> **Уточнення (звірено з кодом):** правило співставлення застосовується **не** під час обробки
> завдання, а **у момент створення проводки** (`action_create_move()` → `_find_matching_rule()`).
> Поле `is_reconciled` правилами **не** заповнюється — це окремий **ручний** прапорець (див. Флоу 5в).

**Рознесення по рахунках.** Рахунок-кореспондент бере **правило співставлення** (крок 3.1),
інакше — транзитний рахунок компанії (`account_journal_suspense_account_id`; якщо його немає —
`ValidationError`). Додатково: експорт виписки у **XLSX**
(`/l10n_ua_bank_sync/export_statement_xlsx/<id>`) і QWeb-звіт виписки.

#### Крок 3.1. Правила авто-співставлення (BANK-6)

**Банк UA → Правила співставлення** (`l10n_ua.bank.match.rule`, `_order = 'sequence, id'`). Правила
перебираються за пріоритетом (`sequence`, менше = вище), **перший збіг виграє** (`_find_matching_rule()`
шукає в межах `company_id`). Метод `check_match(transaction)` перевіряє **всі** умови за логікою **AND**:

| Умова (поле) | Що перевіряє |
|--------------|--------------|
| `journal_id` | якщо задано — правило діє лише для цього банківського журналу |
| `match_direction` | `any` / `incoming` (`amount > 0`) / `outgoing` (`amount < 0`) |
| `match_partner_id` | збіг контрагента транзакції |
| `match_partner_edrpou` | точний збіг ЄДРПОУ контрагента (порівняння зі `.strip()`) |
| `match_text` | підрядок (регістронезалежно) у полі, заданому `match_text_field`: `any` / `payment_ref` / `description` |
| `match_amount_min` / `match_amount_max` | діапазон **абсолютної** суми (`0` = без обмеження) |

Дії при збігу (застосовуються в `action_create_move()`):

| Дія (поле) | Ефект |
|------------|-------|
| `account_id` (обов'язкове) | рахунок-кореспондент замість транзитного |
| `set_partner_id` | перевизначає `partner_id` транзакції |
| `label` | перевизначає найменування рядка проводки |
| `auto_post` | одразу проводить `account.move` після створення |
| `matched_rule_id` | на транзакції фіксується спрацьоване правило |

Кнопка **`Переглянути транзакції`** (`action_view_matched_transactions`) на правилі показує всі
транзакції з цим `matched_rule_id`; лічильник `match_count` рахує збіги.

**Масове застосування.** У списку транзакцій є серверна дія **`Apply Matching Rules & Create Entries`**
(`action_transaction_mass_apply_rules`): для кожної транзакції без проводки шукає правило і, якщо є збіг,
викликає `action_create_move()`.

> _Скриншот:_ `04-match-rules.png`

#### Крок 3.2. Створення, проведення та видалення проводки (BANK-7)

Кнопки на формі транзакції та масові серверні дії у списку:

| Кнопка / дія | Метод | Поведінка |
|--------------|-------|-----------|
| **`Створити проводку`** (стан `new`) | `action_create_move()` | якщо `move_id` вже є — `ValidationError` («Journal entry already exists…»); якщо у журналу немає `default_account_id` — `ValidationError`. Надходження: **Дт банк / Кт кореспондент**; списання: **Дт кореспондент / Кт банк** (`move_type='entry'`) |
| **`Провести`** (стан `draft`) | `action_post_move()` | `move.action_post()` для чернеткових проводок |
| **`Видалити проводку`** (стан `draft`) | `action_delete_move()` | видаляє чернетку; **проведену видалити не можна** → `ValidationError` («Cannot delete posted…») |
| Масово: **Create / Post / Delete Entries** | серверні дії списку | `action_create_and_post_move()` створює й одразу проводить |

> Найменування рядка (`line_label`) береться з `rule.label`, інакше `description` → `payment_ref` → `'/'`.
> Стан транзакції `state` (`new`/`draft`/`posted`) обчислюється зі `move_id.state` і зберігається.

---

## Флоу 3. monobank (токен)

> Імпорт виписки monobank через **Personal Statement API** (модуль `l10n_ua_bank_mono`, провайдер
> `mono`). Логіка ідентична Флоу 2, відрізняється джерелом і токеном. **Наживо на демо не
> проходиться** — потрібен персональний токен monobank.

### Крок 1. Ввести токен у картці конфігурації

**Банк UA → Конфігурації синхронізації → `Новий`**. Заповніть:

| Поле | Значення |
|------|----------|
| Провайдер (`provider`) | **monobank** (`mono`) |
| Банківський журнал (`journal_id`) | банківський журнал monobank |
| **API Token** (`mono_api_token`) | персональний токен (заголовок `X-Token`) |
| **Account ID** (`mono_account_id`) | ID рахунку _(або залишити для автовизначення гривневого)_ |

Натисніть **`Отримати рахунки`** (`action_fetch_accounts()`) — підтягне список рахунків із
балансами через `/personal/client-info`; **`Перевірити підключення`** (`action_test_connection()`)
валідує токен. `_mono_get_default_account()` автоматично визначає гривневий рахунок (валюта **980**).

> **Webhook:** поле `mono_webhook_url` обчислюється (`/l10n_ua_bank_mono/webhook/<id>`) і є прапорець
> `mono_webhook_active`, але **контролер webhook у модулі не реалізований** — real-time оновлення
> поки заглушка. Синхронізація працює через опитування (fetch job).

> **Наживо на демо не виконується** — потрібен реальний персональний токен monobank.

> _Скриншот:_ `06-mono-config.png`

### Крок 2. Завантажити виписку (потрібен токен)

Завдання синхронізації (як у Флоу 2, крок 2):

1. Створіть завдання, вкажіть **період** (обмеження API monobank — **31 день**).
2. **`Завантажити`** — `_fetch_from_bank()`:
   `GET https://api.monobank.ua/personal/statement/{account}/{from}/{to}` із заголовком **`X-Token`**;
   обробляється `429` (rate limit).
3. **`Обробити`** — `_parse_transactions()` переводить суми з **копійок у гривні**, знак задає напрям
   (Дт/Кт), витягує назву / IBAN / ЄДРПОУ контрагента.

> **Наживо на демо не виконується.**

### Крок 3. Результат: транзакції імпортовано, рознесення по рахунках

Результат такий самий, як у Флоу 2, крок 3:

| Показник | Результат |
|----------|-----------|
| Виписка + транзакції | створені за період; суми у гривнях, напрям за знаком |
| Контрагент | назва / IBAN / ЄДРПОУ витягнуті з payload |
| Правила співставлення | проставляють рахунок-кореспондент і контрагента |
| **`Створити проводку`** | `account.move`; рахунок — з правила, інакше транзитний |

> _Скриншот:_ `03-transactions.png`

---

## Флоу 4. Платіжне QR-посилання

> Формування платіжних посилань для рахунків (`account.move`) і замовлень (`sale.order`)
> (модуль `l10n_ua_payment_link`). **QR НБУ працює без ключів і проходиться наживо**; посилання-оплата
> monobank потребує токена еквайрингу.

### Крок 4a. QR НБУ (без ключів — наживо працює)

**Бухгалтерія → Документи → відкрити рахунок.** Поле `nbu_payment_link` формується автоматично за
шаблоном (`ir.config_parameter l10n_ua.nbu_qr_template`, **Постанова НБУ №11 від 01.02.2021**),
кодується `base64url`, посилання — `https://bank.gov.ua/qr/<payload>`.

| Крок | Дія / результат |
|------|-----------------|
| Реквізити | беруться з рахунку/компанії (зовнішнє підключення не потрібне) |
| QR-код | рендериться у SVG data-URI (бібліотека `qrcode`) |
| Друк | **друкована форма рахунку з QR** (`report_invoice_inherit`) містить QR для сплати з банку |

> **Результат:** клієнт сканує QR у застосунку свого банку — реквізити й сума підставляються
> автоматично. Проходиться наживо на демо повністю.

> _Скриншот:_ `09-nbu-qr.png`

### Крок 4b. Еквайринг monobank (потрібен токен мерчанта)

1. У **банківському журналі** (`account.journal`) вкажіть **токен еквайрингу monobank**
   (`mono_acquiring_token`; також доступно через `res.config.settings`).
2. На рахунку/замовленні натисніть **`Створити посилання mono`** (`action_create_mono_link()`):
   `POST https://api.monobank.ua/api/merchant/invoice/create` із токеном журналу; передаються
   позиції рахунку та `webHookUrl`.
3. **Результат:** зберігаються `mono_payment_link` (pageUrl для оплати) і `mono_invoice_id`.

Підтвердження оплати — контролер `/l10n_ua_payment_link/mono/webhook`: за `invoiceId` і
`status == 'success'` знаходить замовлення/рахунок і **проводить оплату**.

> **Наживо на демо не виконується** — потрібен реальний токен мерчанта monobank Acquiring.

> _Скриншот:_ `10-mono-acquiring.png`

---

## Флоу 5. Спільні механізми: планова синхронізація, повторна обробка, звірка

> Наскрізні операційні механізми модуля `l10n_ua_bank_sync`, спільні для ПриватБанку та monobank.
> Живий запуск завантаження на демо потребує токенів (див. застереження вище), але **логіка та кнопки
> проходяться** на будь-якій уже імпортованій виписці.

### Крок 5а. Автозавантаження виписки за розкладом (BANK-9)

На картці конфігурації (`l10n_ua.bank.sync.config`) — блок налаштувань синхронізації:

| Поле | Значення / роль |
|------|-----------------|
| Авто-синхронізація (`auto_sync`) | ☑ вмикає конфіг для планового прогону |
| Інтервал, год (`sync_interval_hours`) | зберігається (типово **24**) |
| Глибина, днів (`default_days_back`) | скільки днів назад тягнути (типово **7**) |
| Остання синхронізація (`last_sync_date`) | час завершення обробки останнього завдання |
| Останнє завдання (`last_sync_job_id`) | посилання на останнє створене завдання |

Три способи запуску:

1. **`Синхронізувати зараз`** (`action_sync_now`) — бере період `today − default_days_back … today`,
   викликає `_create_and_run_job()` (створює `l10n_ua.bank.sync.job`, проставляє `last_sync_job_id`,
   одразу `action_fetch()` → авто-`action_process`), відкриває форму завдання.
2. **Майстер** (`action_open_sync_wizard` → `l10n_ua.bank.sync.wizard`) — довільний період `date_from`/`date_to`.
3. **Крон** — модельний метод `_cron_auto_sync()`: `search([('auto_sync','=',True)])` і для кожного конфігу
   викликає `action_sync_now()` (кожна помилка окремо логується `_logger.error`, не зриваючи решту).

> **Чесне застереження (звірено з кодом):** метод `_cron_auto_sync()` **є**, але **жодного запису
> `ir.cron` у модулях домену не поставляється** — тож «за розкладом» фактично вимагає **вручну створити
> заплановану дію** (Налаштування → Технічне → Заплановані дії) з викликом `model._cron_auto_sync()`.
> Поле `sync_interval_hours` при цьому **не** зчитується самим `_cron_auto_sync` (частоту задає сам `ir.cron`).

> _Скриншот:_ `11-auto-sync-config.png`

### Крок 5б. Повторна обробка та відновлення після помилки (BANK-10)

Сирий payload завдання зберігається (`raw_payload`, `payload_fetched_at`), тож дані переганяються у
транзакції **без повторного звернення до банку**:

| Дія | Метод | Дозволено у стані | Ефект |
|-----|-------|-------------------|-------|
| **`Переобробити`** | `action_reprocess` | будь-який (є payload) | скидає лічильники, ставить `fetched` і повторює `action_process` — зручно **після зміни правил співставлення** |
| **`Завантажити`** | `action_fetch` | `draft` / `error` | re-fetch з банку (інакше `UserError`) |
| **`Обробити`** | `action_process` | `fetched` / `error` | розбір збереженого payload (інакше `UserError`) |
| **`Скинути в чернетку`** | `action_reset_to_draft` | будь-який | стирає payload, лічильники, **видаляє логи** |

Текст помилки зберігається в `error_message`, події — у журналі завдання
(`action_view_logs` → `l10n_ua.bank.sync.log`, рівні debug/info/warning/error). Помилка **окремого**
рядка транзакції логується (`_log`) і не зриває весь пакет (лічильник `error_count`).

> _Скриншот:_ `12-job-logs.png`

### Крок 5в. Звірка руху з контрагентом і рахунками (BANK-8)

**Підв'язка контрагента** відбувається автоматично при обробці — `_match_partner(trans)`:

1. за **ЄДРПОУ** контрагента: `res.partner.search([('edrpou', '=', edrpou)])`;
2. якщо не знайдено — за **IBAN**: `res.partner.bank.search([('acc_number', '=', iban)])` → його `partner_id`.

Знайдений контрагент пишеться в `partner_id` транзакції (сирі дані завжди лишаються в
`partner_name` / `partner_iban` / `partner_edrpou`).

**Прапорець звірки `is_reconciled`** — **ручний**, типово `False`. Керується двома серверними діями у
списку транзакцій: **`Mark as Reconciled`** (`action_transaction_mass_reconcile` → `is_reconciled=True`)
та **`Mark as Not Reconciled`** (`action_transaction_mass_unreconcile` → `False`). У пошуку є фільтри
**`Reconciled`** / **`Not Reconciled`**.

| Крок | Дія / результат |
|------|-----------------|
| Прив'язка контрагента | `_match_partner()` за ЄДРПОУ → IBAN під час обробки завдання |
| Рахунок-кореспондент | правило BANK-6 у `action_create_move()`, інакше транзитний |
| Позначення звіреним | масові дії `Mark as Reconciled` / `Mark as Not Reconciled` (ручні) |
| Зв'язок з інвойсами | сформована `account.move` у банківському журналі співставляється з інвойсами **штатним механізмом звірки Odoo** — окремого коду прив'язки до інвойсів модуль не містить |

> **Чесне застереження:** `is_reconciled` — це **власний прапорець-мітка** модуля, а **не** зв'язок з
> Odoo-reconciliation. Автоматичного зіставлення транзакції з відкритим інвойсом у коді **немає** —
> рознесення на інвойс виконується стандартними засобами Odoo на рівні проводки `account.move`.

> _Скриншот:_ `13-reconcile-filter.png`

---

## Контрольний чек-лист флоу (звірено на демо)

- [x] **Флоу 1 — курси НБУ:** провайдер `nbu` → `Синхронізувати` → записи у `res.currency.rate`, **без ключів** (покрито `test_cross_rate.py`)
- [x] Активні валюти → крос-курс коректний і для не-UAH компанії
- [ ] **Флоу 2 — ПриватБанк:** конфіг `privat` (token + IBAN / merchant) — завантаження виписки потребує зовнішнього токена (на демо не виконується)
- [x] Завдання: `draft → fetching → fetched → processing → done`, повторна обробка без re-fetch
- [x] **BANK-6** — правила: `check_match` (напрям / ЄДРПОУ / текст / діапазон суми / журнал), `sequence` first-match, у межах `company_id`
- [x] **BANK-7** — `action_create_move()` формує `account.move` (Дт/Кт за знаком), dup-guard `ValidationError`, `action_post_move` / `action_delete_move` (проведену не видалити) / `action_create_and_post_move`; правило застосовується **при створенні проводки**, а не при обробці
- [x] **BANK-8** — контрагент через `_match_partner()` (ЄДРПОУ → IBAN); `is_reconciled` — **ручний** прапорець (масові дії + фільтри); зв'язку з інвойсами в коді немає (штатна звірка Odoo на рівні `account.move`)
- [x] **BANK-9** — `auto_sync` + `action_sync_now` / майстер / `_cron_auto_sync()`; ⚠ **запис `ir.cron` не поставляється** — заплановану дію треба створити вручну
- [x] **BANK-10** — `action_reprocess` (без re-fetch), `action_reset_to_draft`, відновлення з `error`, логи `action_view_logs`
- [ ] **Флоу 3 — monobank:** конфіг `mono` (`X-Token`), обмеження 31 день, суми у гривнях — потребує зовнішнього токена (на демо не виконується)
- [x] **Флоу 4 — QR НБУ:** генерується у друкованій формі рахунку без зовнішнього підключення (наживо працює)
- [ ] **Флоу 4 — еквайринг monobank:** посилання-оплата потребує токена мерчанта (на демо не виконується)
- [ ] Скріншоти додати у `img/banks/` (див. імена біля кроків)
