<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Extend backend form for Hotline-specific fields -->
        <record id="view_marketplace_backend_form_hotline" model="ir.ui.view">
            <field name="name">marketplace.backend.form.hotline</field>
            <field name="model">marketplace.backend</field>
            <field name="inherit_id" ref="l10n_ua_marketplace_base.marketplace_backend_view_form"/>
            <field name="arch" type="xml">
                <xpath expr="//page[@name='feed']/group" position="after">
                    <group invisible="marketplace_type != 'hotline'">
                        <group name="hotline_settings" string="Hotline Settings">
                            <field name="hotline_merchant_id"/>
                            <field name="hotline_feed_category_id"/>
                        </group>
                    </group>
                </xpath>
            </field>
        </record>

    </data>
</odoo>
